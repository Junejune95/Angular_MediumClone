import { NavcolordefineDirective } from './navcolordefine.directive';

describe('NavcolordefineDirective', () => {
  it('should create an instance', () => {
    const directive = new NavcolordefineDirective();
    expect(directive).toBeTruthy();
  });
});
